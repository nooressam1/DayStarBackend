export enum DiscountType {
  PERCENTAGE = 'Percentage',
  FIXED_AMOUNT = 'Fixed Amount',
  FREE_SHIPPING = 'Free Shipping',
}

export enum DiscountMinRequirement {
  NONE = 'none',
  AMOUNT = 'amount',
  QUANTITY = 'quantity',
}
